{
    "name": "ts-runtime-check",
    "version": "1.0.0",
    "description": "Flexible, runtime type checking for JSON data",
    "type": "module",
    "script": {
        "test": "tsc && bun test"
    },
    "devDependencies": {
        "@types/bun": "latest",
        "prettier": "^3.3.3"
    },
    "peerDependencies": {
        "typescript": "^5.0.0"
    }
}
